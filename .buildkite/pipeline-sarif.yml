env: 
  file: $file_value

steps:
  - label: ":microscope: SARIF "
    commands:
      - "./scripts/prepare-file.sh"
    plugins:
      - seek-oss/aws-sm#v2.3.1:
          env:
            SARIFT_SECRET: "sarif-token"
      - "ssh://git@github.com/ROKT/upload-sarif-buildkite-plugin.git#a01d3b2422e8c5d217cb820d2ec07a97d65e0b3c":
          repository: 'dotnet-spider'
          sarift: "SARIFT_SECRET"
          commit: ${BUILDKITE_COMMIT}
          file: "$(gzip -c sarif.txt | base64)"
      