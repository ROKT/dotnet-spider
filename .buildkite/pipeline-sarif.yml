steps:
  - label: ":microscope: SARIF "
    plugins:
      - seek-oss/aws-sm#v2.0.0:
          env:
              TOKEN: sarif-token
      - "ssh://git@bitbucket.org/ROKT/upload-sarif-buildkite-plugin.git#cd61d9d3fdbbad712835d0b0f595dbad45831844":
          repository: 'dotnet-spider'
          commit: ${BUILDKITE_COMMIT} # Need to get the commit_sha from an env variable
          token: $${TOKEN}
      - docker#v3.3.0:
          image: alpine
          command: ["ls"]